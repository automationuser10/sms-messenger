// Sample data is no longer used - keeping file for compatibility
// All data now comes from API polling

import { Contact, Message } from '../types';

export const sampleContacts: Contact[] = [];
export const sampleMessages: Message[] = [];